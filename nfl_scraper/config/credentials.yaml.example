# NFL.com API Credentials
# Copy this file to credentials.yaml and add your auth token
# 
# To get a fresh auth token:
# 1. Open https://www.nfl.com in your browser
# 2. Open DevTools (F12)
# 3. Go to Network tab
# 4. Filter by "api.nfl.com"
# 5. Make a request (e.g., navigate to injuries page)
# 6. Find a request to api.nfl.com
# 7. Copy the Authorization header value (Bearer token)
# 8. Paste it below

nfl_api:
  auth_token: "YOUR_BEARER_TOKEN_HERE"
  
  # Token expires periodically. You may need to refresh it.
  # The token is a JWT that contains expiration info.
  # Check expiration: https://jwt.io (decode the token)

